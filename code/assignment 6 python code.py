#Importing statistics module
import statistics 
# creating a sample list
sample =(y=ax+b)
#calculating the mean of sample
m = statistics.mean(sample)
#calculating the variace of sample
print("variance of ample set is% s"
%(statistics.variance(sample.xbar=m)))
